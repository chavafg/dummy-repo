pipeline {
        agent { label 'ubuntu-1604' }
        stages {

                stage('Setup repo environment') {
                        steps {
				sh 'echo get env variables'
				sh 'env'
                        }
                }

	}
}
